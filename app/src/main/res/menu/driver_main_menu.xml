<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/heroesItem"
            android:icon="@drawable/ic_action_heroes"
            android:onClick="onNavigationItemSelected"
            android:title="@string/heroes" />
        <item
            android:id="@+id/creaturesItem"
            android:icon="@drawable/ic_action_creature"
            android:onClick="onNavigationItemSelected"
            android:title="@string/creatures" />
        <item
            android:id="@+id/organizationsItem"
            android:icon="@drawable/ic_action_organizations"
            android:onClick="onNavigationItemSelected"
            android:title="@string/organizations" />
        <item
            android:id="@+id/eventsItem"
            android:icon="@drawable/ic_action_events"
            android:onClick="onNavigationItemSelected"
            android:title="@string/events" />
    </group>

</menu>